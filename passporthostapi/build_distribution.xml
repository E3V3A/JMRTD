<project name="jmrtd" default="dist" basedir=".">
	<description>
      JMRTD host API build file
   </description>

	<!-- Global properties for this build -->
	<property name="src" location="src"/>
	<property name="build" location="build"/>
	<property name="dist"  location="dist"/>

	<target name="init">
		<!-- Create the time stamp -->
		<tstamp/>

		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}"/>
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<!-- Compile the java code from ${src} into ${build} -->
		<javac srcdir="${src}" destdir="${build}"/>
	</target>

	<target name="dist" depends="compile" description="generate the distribution" >
		<!-- Create the distribution directory -->
		<mkdir dir="${dist}/lib"/>

		<!-- Copy bouncy castle jar -->
		<copy todir="${dist}/lib">
			<fileset dir="${java.home}/lib/ext" includes="bcprov*.jar"/>
		</copy>

		<!-- Create the jmrtd jar file -->
		<jar jarfile="${dist}/lib/jmrtd-${DSTAMP}.jar">
			<manifest>
				<attribute name="Main-class" value="sos.mrtd.sample.PassportGUI"/>
				<attribute name="VMOptions" value="-Xms250M -Xmx256M"/>
				<attribute name="Class-path" value="bcprov-jdk16-137.jar"/>
			</manifest>
			<fileset dir="${build}/" includes="**/*.class"/>
		</jar>
	</target>

	<target name="clean" description="clean up" >
		<!-- Delete the ${build} and ${dist} directory trees -->
		<delete dir="${build}"/>
		<delete dir="${dist}"/>
	</target>
</project>
